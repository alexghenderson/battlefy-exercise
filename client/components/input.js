import styled from 'styled-components';

const Input = styled.input`
  appearance: none;
  border: none;
  border-radius: 0;
  background: grey;
  font-size: 2rem;
  text-align: center;
`;

export default Input;
