import styled from 'styled-components';

const Input = styled.input`
  appearance: none;
  background: none;
  border: none;
  border-radius: 0;
  font-size: 2rem;
  text-align: center;
`;

export default Input;
